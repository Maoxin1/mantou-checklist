# 参与贡献

感谢你愿意改进 mantou 个人定投清单。

## 提交问题

请先搜索已有 Issue，并说明：

- 你希望解决的问题，而不只是期望增加的字段
- 使用的手机、浏览器和系统版本
- 可以复现问题的最少步骤
- 预期结果与实际结果

不要上传真实备份、私人日记、账户信息、仓位、金额、令牌或其他秘密。截图前请先替换个人内容。

## 本地开发

```powershell
npm install
npm run serve
npm run check
npm run smoke
npm run build
```

提交 Pull Request 前，请确保语法检查、自动化测试和构建全部通过。

## 设计原则

- 本地优先：默认不依赖账号、后端或云端同步
- 低负担：优先减少每天需要填写的字段
- 可验证：展示行动状态和阶段成果，而不只累计连续天数
- 隐私分层：公开状态与私人原文、仓位、金额分离
- 单一成图：保持 1080 × 1536 输出，避免维护重复模板
- 渐进增强：断网时核心填写、预览、备份和下载仍可使用

涉及存储格式、Service Worker 或备份文件的改动，请说明向后兼容方式，并补充相应测试。
