# 安全与隐私

## 支持范围

当前维护线上版本和仓库默认分支中的最新版。

## 报告问题

如果问题不会暴露私人信息，可以提交 GitHub Issue。涉及可利用漏洞、令牌、私人备份或个人数据时，请不要公开粘贴内容；请先通过维护者的 GitHub 主页联系，并只提供最少必要信息。

## 使用者责任

- 不要把秘密写入公开源码或 `config.json`
- 定期导出本地 JSON 备份，并妥善保存
- 在共享设备上使用后，注意浏览器和设备解锁安全
- 只从可信域名安装 PWA

本项目不提供云端账户或数据恢复服务。浏览器本地数据被清除后，只有使用者此前导出的备份可以恢复。
